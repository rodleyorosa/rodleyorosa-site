// these constants serve to keep the time line inside Work Experience and Education sections always aligned with the icons
// all values in px
// todo: find a better way to create a timeline
const iconPadding = 15;
const iconWidth = 20;
const timelineWidth = 2;
const borderWidth = 6;

export { borderWidth, iconPadding, iconWidth, timelineWidth };
